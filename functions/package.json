{
  "name": "work-diary-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test lib/**/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.5",
    "nodemailer": "^7.0.12"
  },
  "devDependencies": {
    "@types/node": "^22.19.11",
    "@types/nodemailer": "^7.0.11",
    "typescript": "^5.9.3"
  }
}
